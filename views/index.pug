doctype html
html
  head
    title Cashier

    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/lodash.js/4.17.4/lodash.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/autonumeric/1.9.46/autoNumeric.min.js')
    script(src='/cashier.js')

    link(rel='stylesheet', href='/stylesheet.css')

  body
    div#main
      h1 Cashier
      p A JavaScript server side application to simulate a cash register that operates with the user to provide change for transactions.
      div#form
        h3 Make a transaction with this form:
        div.form-group
          label Payment:
          input#payment.currency(type='text',placeholder='Payment',name='payment',min='0')
        div.form-group
          label Price:
          input#price.currency(type='text',placeholder='Price',name='price',min='0')
        div.form-group
          button#go(type='submit') Go
      div#change
      div#drawer
